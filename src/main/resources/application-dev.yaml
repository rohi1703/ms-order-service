server:
  port: 9090

spring:
  datasource:
    url: <preprodenv-db-url fetch from vault>
    driverClassName: org.h2.Driver
    username: <fetch from vault>
    password: <fetch from vault>
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  h2:
    console:
      enabled: true
